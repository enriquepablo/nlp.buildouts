[buildout]
extends = base.cfg

parts +=
    ipython

eggs +=
    ipdb
    ipython

[ipython]
recipe = zc.recipe.egg
eggs = ${buildout:eggs}
scripts = ipython
extra-paths = ${buildout:directory}/parts/pyclips/lib/python2.6/site-packages

[versions]
ipdb = 0.2
ipython = 0.10.1

[buildout]
extends = base.cfg

eggs += nlp.ircbot

parts += ircbot

auto-checkout += nlp.ircbot

[ircbot]
recipe = zc.recipe.egg
eggs = ${buildout:eggs}
scripts = ircbot
extra-paths = ${conf:extra-paths}

[sources]
nlp.ircbot = git git://github.com/enriquepablo/nlp.ircbot.git
